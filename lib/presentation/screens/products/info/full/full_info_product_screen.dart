import 'package:flutter/material.dart';

class FullInfoProductScreen extends StatelessWidget {
  const FullInfoProductScreen({
    super.key,
    this.idProduct,
  });

  final String? idProduct;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
